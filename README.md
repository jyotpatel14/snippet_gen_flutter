# snippet_gen_flutter

This flutter package provides out of the box boiler plate implements for BASIC CRUD implementations of Provider and Bloc Architecture.

A sample command-line application with an entrypoint in `bin/`, library code
in `lib/`, and example unit test in `test/`.
